/**
 * Meta-package barrel — re-exports the full public API from `@glb-compressor/core`.
 *
 * Install `glb-compressor` to get the library, CLI, and server in one package.
 * Or install individual `@glb-compressor/*` packages for finer control.
 *
 * @module glb-compressor
 */
export * from '@glb-compressor/core';
