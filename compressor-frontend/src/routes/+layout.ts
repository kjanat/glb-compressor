/**
 * Prerender all pages for static deployment (GitHub Pages).
 * When switching to SSR later, remove this file or set `prerender = false`.
 */
export const prerender = true;

/**
 * Use SPA-style client-side routing (no SSR).
 * Safe for GitHub Pages; swap to `true` when adding server-side features.
 */
export const ssr = false;
